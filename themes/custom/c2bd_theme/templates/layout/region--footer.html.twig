{#
/**
 * @file
 * Theme override to display a region.
 *
 * Available variables:
 * - content: The content for this region, typically blocks.
 * - attributes: HTML attributes for the region <div>.
 * - region: The name of the region variable as defined in the theme's
 *   .info.yml file.
 *
 * @see template_preprocess_region()
 */
#}
{%
  set classes = [
    'region',
    'region-' ~ region|clean_class,
  ]
%}

  <div{{ attributes.addClass(classes) }}>
    <div class="region-footer--left-side">
      {% if content %}
        {{ content }}
      {% endif %}
    </div>
    <div class="region-footer--right-side">
{#      <span class="footer-title">
        Follow us on Social Media:
      </span>
      <div class="social-block">
        <a href="https://www.google.com/" class="social-icon" aria-label="facebook link" target="_blank"><img src="/themes/custom/c2bd_theme/images/dist/icons/facebook.svg" alt="facebook icon"></a>
      </div>#}
    </div>

  </div>

